- route:
    id: cron-log
    from:
      uri: timer
      parameters:
        timerName: cron
        period: 5000
      steps:
        - setBody:
            expression:
              simple:
                expression: "Scheduled task running at ${date:now:HH:mm:ss}"
        - log:
            message: "${body}"

