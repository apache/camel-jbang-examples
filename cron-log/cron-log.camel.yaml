- route:
    id: cron-log
    from:
      uri: timer
      parameters:
        timerName: cron
        period: "{{cron.period}}"
      steps:
        - setBody:
            expression:
              simple:
                expression: "{{cron.message}} ${date:now:HH:mm:ss}"
        - log:
            message: "${body}"

