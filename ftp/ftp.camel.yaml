- route:
    id: route-1133
    from:
      uri: jms:cheese
      steps:
        - log: "Incoming: ${body}"
        - to:
            uri: ftp
            parameters:
              host: localhost
              passiveMode: true
              password: admin
              port: 2221
              username: admin
