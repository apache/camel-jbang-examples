- route:
    from:
      uri: jms
      parameters:
        destinationName: cheese
      steps:
        - log:
            message: "Incoming: ${body}"
        - to:
            uri: ftp
            parameters:
              host: localhost
              passiveMode: true
              password: admin
              port: 2221
              username: admin

