- rest:
    openApi:
      specification: petstore-api.json
- route:
    from:
      uri: direct
      parameters:
        name: addPet
      steps:
        - to:
            uri: log
            parameters:
              loggerName: info
