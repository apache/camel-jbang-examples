- route:
    id: timer-log
    from:
      uri: timer
      parameters:
        timerName: tick
        period: 1000
      steps:
        - setBody:
            expression:
              simple:
                expression: "Hello Camel! (message #${exchangeProperty.CamelTimerCounter})"
        - log:
            message: "${body}"

